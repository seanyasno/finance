# Database - Local Docker PostgreSQL (recommended for development)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/finance"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/finance"

# Or use your remote Supabase database
# DATABASE_URL="postgresql://..."
# DIRECT_URL="postgresql://..."

# JWT Configuration
# Generate a secure random string (32+ characters) for production
# Example: openssl rand -base64 32
JWT_SECRET=your-random-secret-key-min-32-chars

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Cookie Configuration
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
COOKIE_MAX_AGE=604800000